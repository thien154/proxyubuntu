
centos7
bash <(curl -s "https://raw.githubusercontent.com/thien154/proxyubuntu/master/install.sh")

for ubuntu 22.04

bash <(curl -s "https://raw.githubusercontent.com/thien154/proxyubuntu/master/setup_ubuntu.sh") 

bash <(curl -s "https://raw.githubusercontent.com/thien154/proxyubuntu/master/aws.sh")
ubuntu.ssh
bash <(curl -s "https://raw.githubusercontent.com/thien154/proxyubuntu/master/ubuntu.ssh")
