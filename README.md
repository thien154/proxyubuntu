

bash <(curl -s "https://raw.githubusercontent.com/thien154/proxyubuntu/master/install.sh")

bash <(curl -s "https://raw.githubusercontent.com/thien154/proxyubuntu/master//setup_ubuntu.sh") ubuntu 20
